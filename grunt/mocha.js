/* global module */
module.exports = {
    test: {
        options: {
            urls: ['specs/SpecRunner.html'],
            bail: true,
            log: true,
            run: false,
            reporter: 'Nyan'
        }
    }
};
